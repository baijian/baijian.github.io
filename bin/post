#!/bin/sh
binpath=$(cd `dirname $0`; pwd)
postpath="${binpath}/../_posts"
datetime=$(date +"%Y-%m-%d-")
postname=$datetime$1".markdown"
filename=$postpath/$postname
touch $filename
if [ $? -eq 0 ];then
    echo "\033[32;40mAdd post ${filename} Successful! \033[0m"
else
    echo "\033[31;40mTo add ${filename} Fail! \033[0m"
fi
